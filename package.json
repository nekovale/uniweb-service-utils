{
  "name": "uni_serivce_package",
  "version": "1.0.0",
  "main": "src/index.js",
  "types": "src/types.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "publish-patch": "yarn build && npm version patch && npm publish"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "typescript": "^5.1.6"
  }
}
