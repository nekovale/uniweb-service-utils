export enum Lang {
  EN = "en",
  ZH = "zh",
}

export enum Role {
  ADMIN = 1,
  USER = 2,
  SUPER = 3,
}

export enum Group {
  LIST = 1,
}

export enum Resource {
  TEXT = 1,
  IMAGE = 2,
  VIDEO = 3,
  RICH = 4,
}
